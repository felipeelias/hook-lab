services:
  app:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - db_data:/app/data
    environment:
      - SECRET_KEY_BASE=${SECRET_KEY_BASE}
      - DATABASE_PATH=/app/data/hook_lab.db
      - PHX_HOST=localhost

volumes:
  db_data:
